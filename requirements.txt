# Core LangChain ecosystem (1.2.x)
langchain>=1.2.0,<1.3.0
langchain-core>=1.2.10,<2.0.0
langchain-community>=0.4.1
langchain-openai>=1.1.0
langchain-classic>=1.0.0          # langchain-community 0.4.x 的必要依赖

# LangGraph Agent 框架 (1.0.x)
langgraph>=1.0.8,<1.1.0
langgraph-prebuilt>=1.0.8,<1.1.0  # create_react_agent / ToolNode / ValidationNode
langgraph-checkpoint>=4.0.0        # InMemorySaver（MemorySaver 已重命名）

# Vector store for RAG long-term memory
langchain-chroma>=1.1.0
chromadb>=0.5.0

# HTTP API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database connectivity (for monitoring tools)
sqlalchemy>=2.0.0
pymysql>=1.1.0
psycopg2-binary>=2.9.0

# Redis monitoring
redis>=5.0.0

# Message queue monitoring
pika>=1.3.0  # RabbitMQ
kafka-python>=2.0.0  # Kafka (optional)

# HTTP client
httpx>=0.27.0
aiohttp>=3.10.0

# SSH / remote server operations
paramiko>=3.4.0

# Environment and config
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
httpx>=0.27.0  # For FastAPI test client

# Utilities
tenacity>=8.3.0  # Retry logic
structlog>=24.0.0  # Structured logging
rich>=13.7.0  # Rich output for CLI
PyYAML>=6.0.1
python-multipart>=0.0.9

# Embedding models
sentence-transformers>=3.0.0

# Tiktoken for token counting
tiktoken>=0.7.0
